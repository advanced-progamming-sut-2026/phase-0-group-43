package controller.auth;

import model.user.User;
import model.persistence.UserRepository;

public class LoginValidator {
    public static User validate(String username, String password, UserRepository repo) {
        return null;
    }
}
